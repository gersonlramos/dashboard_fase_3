---
phase: 06-timeline-forecast-view
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/calculations.py
  - app/dashboard/dashboard.py
  - tests/test_forecast_calculations.py
autonomous: true
requirements:
  - FORE-01
  - FORE-06
  - FORE-07
must_haves:
  truths:
    - "A aba '📅 Previsao' aparece na navegacao lateral e abre sem excecao"
    - "Quando throughput diario tem menos de 3 pontos, o motor de previsao retorna fallback (sem quebra)"
    - "Funcoes de previsao ficam em cache e nao recalculam a cada interacao"
  artifacts:
    - path: "app/dashboard/calculations.py"
      provides: "Funcoes puras para Monte Carlo e fallback linear"
      exports: ["monte_carlo_forecast", "forecast_linear_range"]
    - path: "app/dashboard/dashboard.py"
      provides: "Entrada da aba Previsao e wiring inicial"
      contains: "📅 Previsao"
    - path: "tests/test_forecast_calculations.py"
      provides: "Cobertura de comportamento para guardas e percentis"
  key_links:
    - from: "app/dashboard/dashboard.py"
      to: "app/dashboard/calculations.py"
      via: "import de funcoes de previsao"
      pattern: "from calculations import.*monte_carlo_forecast"
    - from: "daily_throughput"
      to: "fallback de projecao"
      via: "guard len(daily_throughput) < 3"
      pattern: "len\(daily_throughput\) < 3"
---

<objective>
Criar a base da aba de previsao com contratos de calculo, guarda de dados insuficientes e cache.

Purpose: Estabelece os contratos de previsao que os planos 06-02 e 06-03 vao consumir sem retrabalho.
Output: Nova aba de navegacao, funcoes de previsao em calculations.py, testes unitarios de previsao, e bloco inicial de fallback seguro.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@app/dashboard/dashboard.py
@app/dashboard/calculations.py

<interfaces>
From app/dashboard/dashboard.py:
```python
aba_selecionada = st.sidebar.radio(
    "Visualize:",
    ["📊 Executivo", "📈 Graficos", "📋 Detalhes", "⚠️ Pendencias", "🗺️ Mapa de Migracao"],
    label_visibility="collapsed"
)
```

From app/dashboard/calculations.py:
```python
def projetar_burndown(ritmo, prazo_limite, historias_faltantes, total_planejado, realizado_atual, ultima_data_real_bh):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir comportamento de previsao por testes (FORE-06, FORE-07)</name>
  <files>tests/test_forecast_calculations.py</files>
  <behavior>
    - Test 1: `monte_carlo_forecast([1,2,3], remaining=20, seed=42)` retorna dict com `p50` e `p85` inteiros, `p85 >= p50 > 0`.
    - Test 2: `monte_carlo_forecast([1,2], remaining=20)` retorna `None` (guarda de dados insuficientes).
    - Test 3: `forecast_linear_range(ritmo=2.0, remaining=10)` retorna offsets para atual/melhor/pior com fator +30% e -30%.
    - Test 4: ritmo zero ou remaining zero retorna estrutura valida sem excecao.
  </behavior>
  <action>Criar testes unitarios para os contratos de previsao em `tests/test_forecast_calculations.py`, sem importar Streamlit. Usar somente funcoes puras de `calculations.py`.</action>
  <verify>
    <automated>python -m pytest tests/test_forecast_calculations.py -q</automated>
  </verify>
  <done>Arquivo de teste criado e inicialmente falhando por ausencia das novas funcoes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar funcoes de previsao e wiring inicial da aba (FORE-01, FORE-06, FORE-07)</name>
  <files>app/dashboard/calculations.py, app/dashboard/dashboard.py</files>
  <behavior>
    - Testes de `test_forecast_calculations.py` passam sem alterar os contratos existentes.
    - Aba lateral inclui `📅 Previsao` apos `🗺️ Mapa de Migracao`.
    - Quando nao houver throughput suficiente, aba mostra `st.info("Dados insuficientes - projecao indisponivel para este lago")`.
  </behavior>
  <action>Adicionar em `calculations.py` as funcoes puras `monte_carlo_forecast(daily_throughput, remaining, n_simulations=5000, seed=42)` e `forecast_linear_range(...)`. Em `dashboard.py`, importar essas funcoes, adicionar item de navegacao `📅 Previsao` (D-ROADMAP) e criar bloco inicial `elif` com guarda de dados insuficientes. Decorar quaisquer funcoes auxiliares de previsao no dashboard com `@st.cache_data(ttl=900)` para cumprir FORE-07.</action>
  <verify>
    <automated>python -m pytest tests/test_forecast_calculations.py -q</automated>
  </verify>
  <done>Contratos de previsao implementados, aba Previsao presente e fallback de dados insuficientes funcional.</done>
</task>

</tasks>

<verification>
Executar:
- `python -m pytest tests/test_forecast_calculations.py -q`
- `python -m pytest tests/test_app_smoke.py -q`
</verification>

<success_criteria>
- Aba `📅 Previsao` aparece na navegacao lateral.
- `monte_carlo_forecast` retorna `None` quando throughput tiver menos de 3 pontos.
- Funcoes de previsao com cache aplicadas (`@st.cache_data(ttl=900)` em helpers do dashboard).
- Nenhuma regressao no smoke test.
</success_criteria>

<output>
After completion, create `.planning/phases/06-timeline-forecast-view/06-01-SUMMARY.md`
</output>
