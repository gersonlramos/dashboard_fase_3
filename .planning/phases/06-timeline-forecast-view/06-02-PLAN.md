---
phase: 06-timeline-forecast-view
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/dashboard/dashboard.py
autonomous: true
requirements:
  - FORE-02
  - FORE-03
  - FORE-05
must_haves:
  truths:
    - "A aba Previsao mostra linha central P50 e faixa de confianca ate P85"
    - "Quando Monte Carlo nao puder ser aplicado, a projecao linear +/-30% aparece como fallback"
    - "Marcos por lake (datas_esperadas_por_lake.csv) aparecem como linhas verticais no grafico"
  artifacts:
    - path: "app/dashboard/dashboard.py"
      provides: "Grafico de previsao com banda, fallback linear e milestone markers"
      contains: "fill='tonexty'"
  key_links:
    - from: "lakes_fase['data_fim']"
      to: "fig forecast"
      via: "add_vline(str(data), annotation_text=lake)"
      pattern: "add_vline"
    - from: "resultado monte carlo"
      to: "render de banda"
      via: "P50 como linha + P85 invisivel com fill tonexty"
      pattern: "tonexty"
---

<objective>
Construir o grafico principal de previsao com banda probabilistica e marcos por lake.

Purpose: Entrega o nucleo visual de FORE-02/03/05 para o time enxergar data central, risco e datas de corte.
Output: Grafico Plotly na aba Previsao com P50/P85, fallback linear +/-30% e linhas de milestone por lake.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/dashboard/dashboard.py

<interfaces>
From app/dashboard/dashboard.py:
```python
fig_burn.add_trace(go.Scatter(... name='Projecao (Melhor)'))
fig_burn.add_trace(go.Scatter(... name='Projecao (Atual)'))
fig_burn.add_trace(go.Scatter(... name='Projecao (Pior)'))
```

Milestone pattern ja existente:

```python
fig_gantt.add_vline(x=str(_dt.date()), line_dash='dot', annotation_text=_lake)
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Montar serie de previsao P50/P85 e fallback linear (FORE-02, FORE-03)</name>
  <files>app/dashboard/dashboard.py</files>
  <behavior>
    - Com throughput suficiente, a aba produz offsets P50/P85 convertidos para datas de conclusao.
    - Com throughput insuficiente, usa serie de fallback linear com fatores 1.3/0.7.
    - Nao altera calculos existentes de burnup/burndown das outras abas.
  </behavior>
  <action>No bloco da aba `📅 Previsao`, derivar throughput diario a partir de `burn_real`/`burn_real_acum` e itens restantes a partir de `total_planejado` vs `realizado_atual`. Aplicar Monte Carlo quando possivel; caso contrario, reutilizar o padrao linear +/-30% (coerente com ROADMAP). Garantir isolamento do codigo dentro da nova aba para evitar regressao das abas antigas.</action>
  <verify>
    <automated>python -m pytest tests/test_app_smoke.py -q</automated>
  </verify>
  <done>Aba calcula datas de previsao central e banda/fallback sem excecao.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar grafico com banda de confianca e milestones por lake (FORE-05)</name>
  <files>app/dashboard/dashboard.py</files>
  <action>Renderizar `go.Figure` com duas traces para banda: trace P50 visivel e trace P85 com `fill='tonexty'` para faixa sombreada. Para fallback linear, manter linha tracejada. Ler `lakes_fase` e adicionar `add_vline` com datas maximas `data_fim` por lake (converter para string) e rotulo do nome do lake. Aplicar hovertemplate com `<extra></extra>` para consistencia visual.</action>
  <verify>
    <automated>python -m pytest tests/test_app_smoke.py -q</automated>
  </verify>
  <done>Grafico com banda P50-P85 e marcos verticais por lake renderiza corretamente na aba Previsao.</done>
</task>

</tasks>

<verification>
Executar:
- `python -m pytest tests/test_app_smoke.py -q`
- `python -m pytest tests/test_forecast_calculations.py -q`
</verification>

<success_criteria>

- Banda P50-P85 visivel para lakes com throughput suficiente.
- Fallback linear +/-30% aparece quando throughput for insuficiente.
- Linhas verticais de milestone aparecem para lakes com `data_fim` valida.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-timeline-forecast-view/06-02-SUMMARY.md`
</output>
