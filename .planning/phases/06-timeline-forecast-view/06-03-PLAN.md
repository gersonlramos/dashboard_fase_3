---
phase: 06-timeline-forecast-view
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - app/dashboard/dashboard.py
autonomous: true
requirements:
  - FORE-02
  - FORE-04
must_haves:
  truths:
    - "Grafico de velocidade mostra media movel 7d e 14d no mesmo periodo"
    - "Cards exibem previsao central (P50), conservadora (P85) e delta de velocidade"
    - "Previsao overall e por epic/lake ficam visiveis na aba sem quebrar filtros existentes"
  artifacts:
    - path: "app/dashboard/dashboard.py"
      provides: "Grafico de tendencia de velocidade e cards finais de previsao"
      contains: "yaxis2"
  key_links:
    - from: "burn_real diario"
      to: "rolling 7d/14d"
      via: "rolling(window=7|14).mean()"
      pattern: "rolling\(7\)|rolling\(14\)"
    - from: "metric cards"
      to: "resultado forecast"
      via: "st.metric(P50, P85, delta 7d-14d)"
      pattern: "st.metric"
---

<objective>
Concluir a aba de previsao com tendencia de velocidade e metricas executivas finais.

Purpose: Fecha o valor de decisao da fase: previsao central/conservadora + sinal de aceleracao/desaceleracao.
Output: Grafico de velocidade 7d/14d em eixo secundario, cards de previsao e visao consolidada por recorte filtrado.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/dashboard/dashboard.py

<interfaces>
From current plotting patterns:
```python
fig.update_layout(
    xaxis=dict(...),
    yaxis=dict(...),
    legend=plotly_legend_style,
)
```

From existing metrics pattern:

```python
c1, c2, c3 = st.columns(3)
c1.metric("...", value)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar grafico de velocidade 7d vs 14d (FORE-04)</name>
  <files>app/dashboard/dashboard.py</files>
  <action>Construir serie diaria de throughput (itens entregues por dia) e calcular medias moveis de 7 e 14 dias. Renderizar grafico com Plotly usando eixo secundario (`yaxis2`) quando combinado ao grafico de previsao ou grafico dedicado com legenda explicita `Velocidade 7d` e `Velocidade 14d`. Evidenciar aceleracao/desaceleracao pela diferenca entre curvas e pelo delta numerico.</action>
  <verify>
    <automated>python -m pytest tests/test_app_smoke.py -q</automated>
  </verify>
  <done>Curvas 7d e 14d aparecem e respondem ao filtro de Data-Lake.</done>
</task>

<task type="auto">
  <name>Task 2: Finalizar cards de previsao e consolidacao por recorte (FORE-02)</name>
  <files>app/dashboard/dashboard.py</files>
  <action>Adicionar cards `Previsao Central (P50)`, `Previsao Conservadora (P85)` e `Delta de Velocidade (7d - 14d)` acima dos graficos. Garantir que os valores sejam derivados do recorte atual (lake selecionado ou todos) e mostrar `N/A` quando nao houver base minima. Preservar fallback de dados insuficientes definido no 06-01.</action>
  <verify>
    <automated>python -m pytest tests/test_app_smoke.py -q</automated>
  </verify>
  <done>Cards de previsao e delta de velocidade visiveis e coerentes com o filtro ativo.</done>
</task>

</tasks>

<verification>
Executar:
- `python -m pytest tests/test_app_smoke.py -q`
- `python -m pytest tests/ -q`
</verification>

<success_criteria>

- Grafico de velocidade com medias moveis 7d/14d operacional.
- Cards P50/P85/delta exibidos acima dos graficos.
- Aba Previsao funciona para `Todos` e para um lake especifico, sem regressao nas abas antigas.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-timeline-forecast-view/06-03-SUMMARY.md`
</output>
