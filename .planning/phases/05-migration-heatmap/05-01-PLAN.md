---
phase: 05-migration-heatmap
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/dashboard.py
autonomous: true
requirements:
  - HEAT-01
  - HEAT-02
  - HEAT-03

must_haves:
  truths:
    - "🗺️ Mapa de Migração appears as the 5th option in the sidebar radio list"
    - "STATUS_COLOR_MAP, STATUS_BUCKET, BUCKET_ORDER, BUCKET_COLORS are defined as module-level constants"
    - "_compute_heatmap_pivot(df) returns (pct_df, count_df) covering 5 buckets across all 9 lakes"
    - "elif aba_selecionada == '🗺️ Mapa de Migração': block exists and runs without error"
  artifacts:
    - path: "app/dashboard/dashboard.py"
      provides: "Nav entry, module-level constants, pivot function, tab stub"
      contains: "Mapa de Migração\nSTATUS_COLOR_MAP\nSTATUS_BUCKET\nBUCKET_ORDER\nBUCKET_COLORS\n_compute_heatmap_pivot"
  key_links:
    - from: "aba_selecionada radio (line 545)"
      to: "elif aba_selecionada == '🗺️ Mapa de Migração': (end of file)"
      via: "sidebar radio string match"
      pattern: "Mapa de Migração"
    - from: "_compute_heatmap_pivot(df_filtrado)"
      to: "pct_df, count_df"
      via: "groupby + unstack + div"
      pattern: "_compute_heatmap_pivot"
---

<objective>
Add navigation entry + module-level constants + pivot helper function for the Migration Heatmap tab.

Purpose: Establishes the foundation that 05-02 (full render) builds on — nav option, data-shaping logic, and the tab stub.
Output: Navigation entry in sidebar, 4 module-level constants, \_compute_heatmap_pivot() function, and elif stub rendering "🗺️ Mapa de Migração" placeholder.
</objective>

<execution_context>
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key existing patterns the executor must replicate exactly -->

From app/dashboard/dashboard.py line 545-549 (current radio — ADD 5th item):

```python
aba_selecionada = st.sidebar.radio(
    "Visualize:",
    ["📊 Executivo", "📈 Gráficos", "📋 Detalhes", "⚠️ Pendências"],
    label_visibility="collapsed"
)
```

From app/dashboard/dashboard.py line 74-103 (theme block — add constants AFTER this block):

```python
plotly_template = "plotly_white" if tema_selecionado == "☀️ Claro" else "plottly_dark"
plotly_font_color = ...
plotly_paper_bgcolor = ...
plotly_plot_bgcolor = ...
plotly_axis_style = dict(...)
plotly_legend_style = dict(...)
```

From app/dashboard/dashboard.py line 1480 (pattern for tab rendering block):

```python
if aba_selecionada == "📊 Executivo":
    ...
elif aba_selecionada == "📈 Gráficos":
    ...
elif aba_selecionada == "📋 Detalhes":
    ...
elif aba_selecionada == "⚠️ Pendências":
    ...
# ← add elif here at end of file (after line 2401)
```

Real status values in FASE_3.csv:
Canceled, Done, In Test, In progress, Open, To Do, Waiting Test

Real Data-Lake values (uppercase):
BMC, CLIENTE, COMERCIAL, COMPRAS, FINANCE, MOPAR, RH, SHARED SERVICES, SUPPLY CHAIN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nav entry + module-level constants</name>
  <read_first>
    - app/dashboard/dashboard.py (lines 74-110 for theme block end; lines 544-552 for radio definition)
  </read_first>
  <files>app/dashboard/dashboard.py</files>
  <action>
Make two separate edits to app/dashboard/dashboard.py:

**Edit 1 — sidebar radio (line 547):**
Change the radio list from 4 to 5 options by appending "🗺️ Mapa de Migração":

```python
aba_selecionada = st.sidebar.radio(
    "Visualize:",
    ["📊 Executivo", "📈 Gráficos", "📋 Detalhes", "⚠️ Pendências", "🗺️ Mapa de Migração"],
    label_visibility="collapsed"
)
```

**Edit 2 — module-level constants (insert after the plotly_legend_style block, ~line 103):**
Add these 4 constants as a clearly labelled block:

```python
# ── Heatmap: constantes de bucket e cor ─────────────────────────────────────
STATUS_COLOR_MAP = {
    'Done':       '#90EE90',
    'Closed':     '#90EE90',
    'Resolved':   '#90EE90',
    'In progress': '#87CEEB',
    'In Test':    '#87CEEB',
    'Waiting Test': '#87CEEB',
    'Open':       '#D3D3D3',
    'To Do':      '#D3D3D3',
    'Canceled':   '#FFD700',
    'Cancelled':  '#FFD700',
    'Cancelado':  '#FFD700',
    'Bloqueado':  '#FF6B6B',
}

STATUS_BUCKET = {
    'Open':         'Não Iniciado',
    'To Do':        'Não Iniciado',
    'In progress':  'Em Andamento',
    'In Test':      'Em Andamento',
    'Waiting Test': 'Em Andamento',
    'Done':         'Concluído',
    'Closed':       'Concluído',
    'Resolved':     'Concluído',
    'Canceled':     'Cancelado',
    'Cancelled':    'Cancelado',
}

BUCKET_ORDER  = ['Não Iniciado', 'Em Andamento', 'Concluído', 'Cancelado', 'Bloqueado']
BUCKET_COLORS = ['#D3D3D3',      '#87CEEB',       '#90EE90',   '#FFD700',   '#FF6B6B']
# Ordem e cores alinhadas: index 0=Não Iniciado, 1=Em Andamento, 2=Concluído, 3=Cancelado, 4=Bloqueado
```

  </action>
  <acceptance_criteria>
    - grep -n "Mapa de Migração" app/dashboard/dashboard.py returns at least 1 hit on the radio line (the list now has 5 items)
    - grep -n "STATUS_COLOR_MAP" app/dashboard/dashboard.py returns at least 1 hit
    - grep -n "STATUS_BUCKET" app/dashboard/dashboard.py returns at least 1 hit
    - grep -n "BUCKET_ORDER" app/dashboard/dashboard.py returns at least 1 hit
    - grep -n "BUCKET_COLORS" app/dashboard/dashboard.py returns at least 1 hit
  </acceptance_criteria>
  <done>5th radio item added; 4 module-level constants defined after theme vars block.</done>
</task>

<task type="auto">
  <name>Task 2: Add _compute_heatmap_pivot function + elif stub</name>
  <read_first>
    - app/dashboard/dashboard.py (lines 360-370 for _render_indicadores placement; lines 2390-2401 for end of file)
  </read_first>
  <files>app/dashboard/dashboard.py</files>
  <action>
Make two separate edits to app/dashboard/dashboard.py:

**Edit 1 — \_compute_heatmap_pivot helper function:**
Insert the function after the `_render_indicadores` helper function (which starts at line 368). Place it right after `_render_indicadores` definition ends, before the data-loading section (~line 478). The function must use the module-level constants defined in Task 1:

```python
def _compute_heatmap_pivot(df):
    """Retorna (pct_df, count_df) para o heatmap de migração.

    Linhas = Data-Lakes ordenados; colunas = BUCKET_ORDER (5 buckets).
    pct_df: percentual de itens por célula (0-100, uma casa decimal).
    count_df: contagem absoluta de itens por célula.
    """
    _df = df.copy()
    _df['_bucket'] = (
        _df['Status'].astype(str).str.strip()
        .map(STATUS_BUCKET)
        .fillna('Bloqueado')
    )
    count_df = (
        _df.groupby(['Data-Lake', '_bucket'])
        .size()
        .unstack(fill_value=0)
        .reindex(columns=BUCKET_ORDER, fill_value=0)
    )
    _total = count_df.sum(axis=1).replace(0, 1)  # evita divisão por zero
    pct_df = (count_df.div(_total, axis=0) * 100).round(1)
    _lakes = sorted(count_df.index.tolist())
    return pct_df.reindex(_lakes), count_df.reindex(_lakes)
```

**Edit 2 — elif stub at end of file:**
Append after the last line of the `elif aba_selecionada == "⚠️ Pendências":` block (end of file):

```python
elif aba_selecionada == "🗺️ Mapa de Migração":
    st.subheader("🗺️ Mapa de Migração")
    st.markdown(hr_style, unsafe_allow_html=True)

    _pct_df, _count_df = _compute_heatmap_pivot(df_filtrado)

    # Renderização completa no plano 05-02
    st.info("Heatmap em construção — execute o plano 05-02 para a renderização completa.")
```

  </action>
  <acceptance_criteria>
    - grep -n "_compute_heatmap_pivot" app/dashboard/dashboard.py returns at least 2 hits (definition + call in elif)
    - grep -n "elif aba_selecionada.*Mapa de Migração" app/dashboard/dashboard.py returns 1 hit
    - python -m pytest tests/test_smoke.py -q exits 0 (app loads without exception)
    - python -m pytest tests/ -q exits 0 (116 tests pass — no regressions)
  </acceptance_criteria>
  <done>_compute_heatmap_pivot defined; elif stub appended at end of file; smoke test passes.</done>
</task>

</tasks>

<verification>
```bash
# Nav entry present
grep -n "Mapa de Migração" app/dashboard/dashboard.py

# All constants defined

grep -n "STATUS_COLOR_MAP\|STATUS_BUCKET\|BUCKET_ORDER\|BUCKET_COLORS" app/dashboard/dashboard.py

# Pivot function and tab stub

grep -n "\_compute_heatmap_pivot\|elif.\*Mapa" app/dashboard/dashboard.py

# No regressions

python -m pytest tests/ -q

```
</verification>

<success_criteria>
- "🗺️ Mapa de Migração" is the 5th item in the sidebar radio list
- STATUS_COLOR_MAP, STATUS_BUCKET, BUCKET_ORDER, BUCKET_COLORS defined at module level after theme vars
- _compute_heatmap_pivot(df) defined and callable; returns two DataFrames with 5 columns (BUCKET_ORDER)
- elif stub renders without exception
- All 116 existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-migration-heatmap/05-01-SUMMARY.md`
</output>
```
